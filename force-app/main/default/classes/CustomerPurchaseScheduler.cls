public without sharing class CustomerPurchaseScheduler
    implements Schedulable {

    public void execute(SchedulableContext context) {

        Database.executeBatch(
            new CustomerPurchaseBatch(),
            200
        );
    }
}